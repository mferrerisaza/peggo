<%= simple_form_for(share) do |f| %>
  <td>
    <%= f.association :owner, collection: current_user.owners, label: false %>
  </td>
  <td>
    <%= f.input :ownerability_percentage, label: "Porcentaje de participación",  input_html: { step: 0.001 }, placeholder: "Ej: 0.1", label: false%>
  </td>
  <td>
    <%= f.input :payment_percentage, label: "Porcentaje de pago de cuota",  input_html: { step: 0.001 }, placeholder: "Ej: 0.1", label: false%>
  </td>
  <td>
    <%= f.association :property, collection: @building.properties, input_html: { value: @property.id }, as: :hidden%>
    <%= button_tag(type: 'submit') do %>
      <i class="material-icons">
        check
      </i>
    <% end %>
    <%= button_tag class: "show-edit-share-form" do %>
      <i class="material-icons">
        clear
      </i>
    <% end %>
  </td>
<% end %>
